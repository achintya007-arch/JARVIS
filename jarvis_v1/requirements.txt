# Single source of truth for dependencies is pyproject.toml.
# This file installs the pinned application deps defined there.
#
#   pip install -r requirements.txt
#
# For optional feature sets:
#   pip install -e ".[vision]"   # YOLO object detection (heavy, pulls torch)
#   pip install -e ".[dev]"      # pytest, ruff, mypy
-e .
